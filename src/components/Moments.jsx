import { images } from '../data';
export default function Moments() { const cards = [[images.airport, 'Airport Rentals'], [images.family, 'Family Trips'], [images.tour, 'Long Tours']]; return <section className="section moments"><div className="container"><div className="section-head centered reveal"><p className="eyebrow">TRAVEL WITH PURPOSE</p><h2>More than miles —<br /><span>we bring people together.</span></h2></div><div className="moment-grid">{cards.map(([im, title], i) => <div className={`moment-card reveal m${i}`} key={title}><img src={im} alt={title} loading="lazy" /><div><span>0{i + 1}</span><h3>{title}</h3></div></div>)}</div></div></section>; }
