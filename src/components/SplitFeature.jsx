import { ArrowRight, ShieldCheck } from 'lucide-react';
export default function SplitFeature({ reverse = false, title, eyebrow, text, image, button }) { return <section className={`split-section ${reverse ? 'reverse' : ''}`}><div className="container split-grid"><div className="split-image reveal"><img src={image} alt="" loading="lazy" /><div className="image-tag"><ShieldCheck size={16} /> Built for peace of mind</div></div><div className="split-copy reveal"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p>{text}</p><a className="text-link" href="#app">{button} <ArrowRight size={17} /></a></div></div></section>; }
